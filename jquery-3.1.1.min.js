
    var time = new Date();
    var hour = document.getElementById('hour');
    hour.style.WebkitTransform = "rotate(" + ((time.getHours() + time.getMinutes()/60) % 12 * 30)  + "deg)";
    
    var minute = document.getElementById('minute');
    minute.style.WebkitTransform = "rotate(" + (time.getMinutes() + time.getSeconds()/60) * 6  + "deg)";
    
    var second = document.getElementById('second');
    second.style.WebkitTransform = "rotate(" + time.getSeconds() * 6  + "deg)";
    
    var hr = setInterval(function() {
      time = new Date();
      var hour = document.getElementById('hour');
      hour.style.WebkitTransform = "rotate(" + ((time.getHours() + time.getMinutes()/60) % 12 * 30)  + "deg)";
    }, 60000);
    
    var mn = setInterval(function() {
      time = new Date();
      var minute = document.getElementById('minute');
      minute.style.WebkitTransform = "rotate(" + (time.getMinutes() + time.getSeconds()/60) * 6  + "deg)";
    }, 6000);
    
    var sc = setInterval(function() {
      time = new Date();
      var second = document.getElementById('second');
      second.style.WebkitTransform = "rotate(" + time.getSeconds() * 6  + "deg)";
    }, 500);
